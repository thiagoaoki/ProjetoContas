<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

   <TableLayout
      android:layout_height="fill_parent"
      android:layout_width="fill_parent" 
      android:layout_centerHorizontal="true">
      <TableRow>  
         <Button
            android:id="@+id/button1"
            android:onClick="onClick"            
            android:text="Display second activity" />
      </TableRow>
      <TableRow> 

        <Button
            android:id="@+id/btnCredito"
            android:layout_alignLeft="@+id/btnCredito"
            android:layout_below="@+id/btnCredito"
            android:text="Crédito"
            android:onClick="onClick" />

      </TableRow>
      <TableRow> 
        <Button
            android:id="@+id/btnDebito"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@+id/btnDebito"
            android:layout_below="@+id/btnDebito"
            android:text="Débito"
            android:onClick="onClick" />
      </TableRow>          
   </TableLayout>    
  
</RelativeLayout>